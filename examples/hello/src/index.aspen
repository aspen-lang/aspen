import std/runtime (type Syscall).
import hello/output (printer).

export let main = {
  def start: Syscall syscall =>
    printer print: "hello" using: syscall.
    printer print: "world" using: syscall.
}.
