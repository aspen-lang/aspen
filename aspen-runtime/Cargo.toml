[package]
name = "aspen-runtime"
version = "0.1.0"
authors = ["Emil Broman <emil@emilbroman.me>"]
edition = "2018"

[lib]
name = "aspen_runtime"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[features]
standalone = []

[dependencies]
libc = "0.2.70"
